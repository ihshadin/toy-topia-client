import React from 'react';
import useDynamicTitle from '../../../hooks/useDynamicTitle';

const Blogs = () => {
    useDynamicTitle();
    return (
        <div>
            Blog page
        </div>
    );
};

export default Blogs;